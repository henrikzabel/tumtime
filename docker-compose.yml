services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tumtime
      POSTGRES_PASSWORD: tumtime
      POSTGRES_DB: tumtime
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
